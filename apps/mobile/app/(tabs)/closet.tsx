import { Placeholder } from '@/components/Placeholder';

export default function ClosetScreen() {
  return (
    <Placeholder
      title="My closet"
      subtitle="Browse items + combinations"
      mockupLabel="02 · CLOSET · COMBINATIONS"
    />
  );
}
