{
  "name": "@mei/image-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Closet photo cleanup + thumbnail gen + try-on composition. Storage-webhook-driven. See SPEC.md §9.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@mei/types": "workspace:*",
    "@supabase/supabase-js": "^2.45.4",
    "fastify": "^4.28.1",
    "sharp": "^0.33.5",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
