{
  "name": "@mei/infra",
  "version": "0.0.0",
  "private": true,
  "description": "Mei infra (post-CHANGES-02). Holds the upcoming Lambda + Render configs; the original AWS CDK stack is archived under _archive/aws-cdk/ for reference.",
  "scripts": {
    "synth:archive": "cd _archive/aws-cdk && npx cdk synth",
    "diff:archive": "cd _archive/aws-cdk && npx cdk diff",
    "typecheck:archive": "cd _archive/aws-cdk && tsc --noEmit",
    "typecheck": "echo 'no live infra to typecheck yet — see _archive/aws-cdk for the AWS CDK stack, or wait for feat/lambdas-deploy'"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.165.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "aws-cdk": "^2.165.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
